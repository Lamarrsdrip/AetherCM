import AppShell from '@/components/AppShell'
import { markets } from '@/lib/data'
export default function Markets(){return <AppShell><section className="workspace"><div className="workspaceHead"><div><span className="eyebrow">Markets</span><h1>Find what matters.</h1><p>Stocks, ETFs and digital assets in a focused watch experience.</p></div><input className="searchBox" placeholder="Search symbol or company"/></div><div className="marketTable"><div className="tableHead"><span>Asset</span><span>Type</span><span>Price</span><span>Today</span><span></span></div>{markets.map(m=><div className="tableRow" key={m.symbol}><div><span className="assetBadge">{m.symbol[0]}</span><div><b>{m.symbol}</b><small>{m.name}</small></div></div><span>{m.type}</span><b>${m.price.toLocaleString()}</b><span className={m.move>=0?'good':'bad'}>{m.move>=0?'+':''}{m.move}%</span><button className="ghostBtn">Trade</button></div>)}</div></section></AppShell>}
