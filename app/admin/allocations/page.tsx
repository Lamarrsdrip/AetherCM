"use client"
import{useEffect,useState}from"react";import AdminShell from"@/components/AdminShell";import type{ShareAllocation}from"@/lib/ops"
export default function AllocationsAdmin(){const[ops,setOps]=useState<any>(null);useEffect(()=>{fetch("/api/admin/operations").then(r=>r.json()).then(setOps)},[]);const op=async(body:any)=>{const r=await fetch("/api/admin/operations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(r.ok)setOps(await r.json())};return <AdminShell><section className="adminPageV2"><div className="pageHeadV2"><div><span>Investing</span><h1>Allocation requests</h1><p>Review what each client wants to acquire.</p></div></div><div className="requestCardsV2">{ops?.allocations?.map((a:ShareAllocation)=><article key={a.id}><div><b>{a.userName}</b><small>{a.symbol} · {a.shares} shares</small></div><strong>${a.value.toLocaleString('en-US')}</strong><span className={a.status.toLowerCase()}>{a.status}</span>{a.status==="Pending"&&<div><button onClick={()=>op({action:"allocation-status",id:a.id,status:"Approved"})}>Approve</button><button className="dangerV2" onClick={()=>op({action:"allocation-status",id:a.id,status:"Rejected"})}>Reject</button></div>}</article>)}</div></section></AdminShell>}
